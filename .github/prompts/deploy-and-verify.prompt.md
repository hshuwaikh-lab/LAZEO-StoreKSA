---
description: "Build, deploy, and verify the LAZEO-StoreKSA GitHub Pages site"
---

Build the current workspace, publish the generated `dist` folder using the existing GitHub Pages deployment setup, and verify that the deployed output matches the repo slug path.

Use the project’s existing deploy scripts and do not change source code unless the build or publish flow fails.
